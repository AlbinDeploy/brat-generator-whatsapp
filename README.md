# Brat-Inspired Next.js Generator

## Run
npm install
npm run dev

## Deploy
Push to GitHub, then import into Vercel.

## WhatsApp note
Browser apps can export/share the generated file, but cannot silently add it into WhatsApp's sticker tray.
